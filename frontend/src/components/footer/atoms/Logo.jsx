import React from 'react';
import { Typography } from '@mui/material';

const Logo = () => {
  return (
    <Typography variant="h6" sx={{ fontWeight: 700 }}>
      Velody
    </Typography>
  );
};

export default Logo;